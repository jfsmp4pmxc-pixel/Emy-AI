name: EmptyStudioAI
options:
  bundleIdPrefix: com.emptystudio
  objectVersion: 56
  xcodeVersion: "15.0" # Ép thêm dòng này để xcodegen biết đường xử lý đúng format
targets:
  EmptyStudioAI:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - EmptyStudioAI
    settings:
      CODE_SIGNING_ALLOWED: NO